import { RequestObseverResolver } from "../core/resolvers/request-obsever-resolver";

export class RequestObseverConfig{
    public static registerObsever(obseverResolver: RequestObseverResolver){
    }
}